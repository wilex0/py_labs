# Лабораторная работа №5: Функции как аргументы. Стратегии и делегаты.

## Цель работы
Освоить передачу функций как аргументов, применение `map`, `filter`, `reduce`, паттерн «Стратегия», `lambda`-выражения и интеграцию функционального стиля с ООП.

## Реализованные функции и стратегии

### Стратегии сортировки (ключи)
| Функция | Описание |
|---------|----------|
| `by_name` | сортировка по названию продукта |
| `by_seller` | сортировка по продавцу |
| `by_cost` | сортировка по базовой стоимости |
| `by_curr_cost` | сортировка по текущей стоимости (со скидкой) |
| `by_quantity` | сортировка по количеству на складе |
| `by_discount` | сортировка по размеру скидки |
| `by_id` | сортировка по ID продукта |

### Функции-фильтры (предикаты)
| Функция | Описание |
|---------|----------|
| `has_discount` | только товары со скидкой |
| `is_available` | только доступные товары (количество > 0) |
| `is_food` | только продукты питания |
| `is_digital` | только цифровые продукты |

### Фабрики функций
| Фабрика | Что возвращает |
|---------|----------------|
| `make_cost_filter(max)` | фильтр по максимальной стоимости |
| `make_quantity_filter(min, max)` | фильтр по диапазону количества |
| `make_discount_applier(percent)` | функция для применения скидки |

### Callable-объекты (паттерн Стратегия)
- `DiscountStrategy` — стратегия применения скидки, можно менять процент без изменения кода
- `PrintStrategy` — стратегия печати информации о продукте (подробный/краткий режимы)
- `UpdateStrategy` — стратегия обновления скидок для FoodProduct и DigitalProduct

### Методы коллекции `FunctionalProductCollection`
| Метод | Описание |
|-------|----------|
| `sort_by(key_func, reverse)` | сортировка по стратегии (in-place) |
| `filter_by(predicate)` | фильтрация |
| `apply(func)` | применение функции ко всем элементам |
| `for_each(func)` | выполнение функции для каждого элемента |
| `map(transform_func)` | преобразование через `map()` |
| `reduce(func, initial)` | свёртка коллекции |
| `any(predicate)` | проверка наличия элемента |
| `all(predicate)` | проверка всех элементов |
| `total_cost()` | суммарная стоимость |
| `average_cost()` | средняя стоимость |
| `total_discount_saved()` | общая экономия от скидок |
| `first(predicate=None)` | первый элемент (с фильтром) |
| `last(predicate=None)` | последний элемент (с фильтром) |
| `take(n)` | первые n элементов |
| `skip(n)` | пропустить n элементов |
| `distinct_by(key_func)` | уникальные элементы по ключу |

## Демонстрация работы

### Демо 1: Сортировка
- Сортировка по названию, цене, скидке
- Сортировка по продавцу и названию (составной ключ)
#[](../../images/lab05/c1.png)

### Демо 2: Фильтрация
- Фильтрация по доступности товаров
- Фильтрация по наличию скидки
- Фильтрация по типу продукта (питание, цифровые)
#[](../../images/lab05/c2.png)

### Демо 3: Фабрики функций
- Создание фильтра через `make_cost_filter`
- Создание фильтра через `make_quantity_filter`
- Применение скидки через `make_discount_applier`
#[](../../images/lab05/c3.png)

### Демо 4: Цепочки методов
- Цепочка `filter_by => sort_by`
- Цепочка `filter_by => filter_by => sort_by`
- Цепочка `filter_by => apply => sort_by`
#[](../../images/lab05/c4.png)

### Демо 5: Map и lambda
- Извлечение названий продуктов
- Извлечение (имя, цена, скидка)
- Форматирование строк
- Извлечение ID продуктов
#[](../../images/lab05/c5.png)

### Демо 6: Reduce и агрегация
- Подсчёт общей стоимости
- Вычисление средней цены
- Нахождение максимальной цены
- Подсчёт количества товаров со скидкой
- Вычисление общей экономии от скидок
#[](../../images/lab05/c6.png)

### Демо 7: DiscountStrategy
- Базовое применение скидки к продукту
- Изменение процента скидки в рантайме
- Применение скидки ко всей коллекции
#[](../../images/lab05/c7.png)

### Демо 8: PrintStrategy
- Краткий режим печати (имя и цена)
- Подробный режим печати (полная информация)
- Вывод отдельного продукта в разных режимах
#[](../../images/lab05/c8.png)

### Демо 9: UpdateStrategy
- Обновление скидок для продуктов питания
- Обновление скидок для цифровых продуктов
- Повторное обновление (ничего не меняется)
#[](../../images/lab05/c9.png)

### Демо 10: Новые методы коллекции
- `first()` и `first(predicate)` — получение первого элемента
- `last()` — получение последнего элемента
- `take(n)` — первые n элементов
- `skip(n)` — пропуск n элементов
- `distinct_by()` — уникальные элементы по ключу
#[](../../images/lab05/c10.png)

### Демо 11: any и all
- Проверка наличия товаров со скидкой
- Проверка наличия доступных товаров
- Проверка всех товаров на доступность
- Проверка наличия просроченных продуктов
#[](../../images/lab05/c11.png)